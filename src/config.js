const config = {
  defaultPath: '/dashboard/default',
  fontFamily: ``,
  miniDrawer: false,
  container: true,
  mode: 'light',
  themeDirection: 'ltr'
};

export default config;
export const drawerWidth = 260;
